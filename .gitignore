# ---------- 原始素材：体积大 / 非页面运行所需 ----------
*.mp4
*.xlsx
# 游戏数据表（tower_skin_sets.csv）：页面数据已内联进 index.html，
# 重建管线也能从素材转储直接读取，故不入库
*.csv

# ---------- 构建管线与验证产物（本地保留，不随页面发布）----------
# build/ 内含下载转存的 .sc/.sctx 与渲染脚本，共约 48 MB
build/
_verify.html
_shot.png

# ---------- Python ----------
__pycache__/
*.pyc

# ---------- 本地工具目录（工作记忆等，非交付内容）----------
.workbuddy/
